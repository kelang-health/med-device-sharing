# med-device-sharing follow-up — no production change (25 September 2026)

- This project was rechecked read-only as part of the broader Cloud-to-Local plan. Its Production database still records 46 migrations, 25 public tables, no public tables with RLS disabled, and no lock waiter at the spot check; database size was approximately 15,830,163 bytes. These readings are not proof of continuous uptime or complete security.
- Only one authorized Remote Desktop device was connected. No independent approved off-device backup location was established, and **no** borrower/equipment/maintenance records were exported, copied to OSM-PHC's Local archive, deleted from Cloud or moved into a generic cloud drive. OSM-PHC's 10,474-entry archive and isolated house/member test are not med-device backup or app-compatibility evidence.
- Existing med-device Draft PR #1 is still unmerged and intentionally blocks deployment pending complete vetted historical source, Supabase-compatible full replay and authenticated real loan/return, equipment-availability, patient-reference, audit/LINE and least-privilege regression tests.
- The Phase 4 med-device synthetic offline CI checks previously passed; no new Production DDL, Auth, row-level grants, website or gateway code was changed. Guaranteed Cloud storage reclaimed: zero.
- Next: approved encrypted independent med-device archive/restore only after its specific source, access controls and retention rules are established; identify authentic frontend and gateway reads; test a synthetic closed-period Local reporting aggregate with proper staff/admin scopes, offline-local fallback, real-device response-time baseline, and no disruption to active borrowing/returns. Require separate authorizations for an isolated cost-bearing Supabase environment and any Production alteration.


## Continued med-device source and anonymous browser audit

Read-only Supabase migration-history inspection confirms SQL text is retained for all 46 med-device migration entries (approximately 195,240 bytes in total); heuristic checks flag 34 entries as possibly data-changing and 14 as potentially sensitive/auth-related, with overlap possible. The raw borrower/loan import SQL was NOT exported. Metadata-only version/name/count/size/MD5 and heuristic flags were recorded in a restricted non-web-served Local index (shared index with 191 PHC versions). One preliminarily low-risk med-device recorded SQL item was saved as a private non-executable quarantine JSON and verified against the recorded text MD5; this is NOT a complete or approved migration source. No med-device migration was replayed.

The documented GitHub Pages med-device landing and index.html each returned HTTP 200. A fresh browser profile with no stored credentials rendered the public page (document.readyState=complete, 15 resource entries, zero severe console messages in one observed load, navigation load event ~858 ms). The browser test did NOT authenticate or exercise real borrower, loan/return, patient lookup, staff/admin scopes, LINE integration or API backends; the single navigation timing is not p95 or a performance guarantee.

Both C: and D: are partitions of the one available NVMe disk. No authorized off-device encrypted backup destination was connected, so no sensitive med-device data were exported, uploaded or copied; the OSM-PHC archive is not a med-device backup. Production website, Auth, migration history, RLS, loan operations and Cloud rows remain unchanged. The draft audit PR is not approved for merge/deploy; off-device destination, audited historical SQL, isolated complete replay and authenticated real-app test accounts remain blocking gates.
